#!/bin/bash
# Verify write and read operations for VTK format stored using binary data types
# Note that this test does not necessarily guarantee that the binary VTKs are correct;
#   only that the write and read functions work and are self-consistent
# Ensure that output matches that generated by a prior software version
meshconvert meshconvert/in_ascii.vtk tmp.vtk -binary -force
testing_diff_mesh tmp.vtk meshconvert/in_ascii.vtk 0.001
