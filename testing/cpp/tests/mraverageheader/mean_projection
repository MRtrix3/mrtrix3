#!/bin/bash
# Verify operation when the average header voxel spacing uses the "mean_projection" calculation
#   (the default)
# Ensure that output matches that generated by a prior software version
mraverageheader moving.mif.gz template.mif.gz - | \
testing_diff_header - mraverageheader/mean_projection.mif.gz
