#!/bin/bash
# Verify operation of the median filter
# Ensure that output matches that generated by a prior software version
maskfilter mask.mif median -extent 3,3,3 - | \
testing_diff_image - maskfilter/out9.mif
