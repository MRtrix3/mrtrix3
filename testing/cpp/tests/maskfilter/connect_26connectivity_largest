#!/bin/bash
# Verify operation of the connected components filter,
#   when two modifying command-line options are used in conjunction:
#   - "-connectivity" to use 26-nearest-neighbour connectivity rather than 6
#   - "-largest" to extract only the largest connected component
# Ensure that output matches that generated by a prior software version
maskfilter components_mask.mif connect -connectivity -largest - | \
testing_diff_image - maskfilter/out4.mif
