#!/bin/bash
# Verify operation of the connected components filter
#   when the -axes option restricts the axes along which nearest neighbour expansion can occur
# Ensure that output matches that generated by a prior software version
maskfilter components_mask.mif connect -axes 0,2 - | \
testing_diff_image - maskfilter/out5.mif
