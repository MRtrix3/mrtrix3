#!/bin/bash
# Verify operation when both performing panchromatic sharpening using the -contrast option,
#   and masking the input FOD image using the -mask option
# Ensure that output matches that generated by a prior software version
fod2dec fod2dec/fod.mif - \
-contrast fod2dec/anat.mif \
-mask fod2dec/roi.mif | \
testing_diff_image - fod2dec/out/dec-anat_masked.mif -voxel 1e-5
