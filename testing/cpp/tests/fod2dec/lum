#!/bin/bash
# Verify operation when "-lum" command-line option is specified,
#   engaging luminance / perception correction
# Ensure that output matches that generated by a prior software version
fod2dec fod2dec/fod.mif - -lum | \
testing_diff_image - fod2dec/out/dec_lum.mif -voxel 1e-5
