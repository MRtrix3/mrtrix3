#!/bin/bash
# Verify "rms" (root mean square) operator when applied across an axis of a single image
# Outcome is checked against that generated by a prior software version
mrmath dwi.mif rms -axis 3 - | \
testing_diff_image - mrmath/out2.mif -frac 1e-5
