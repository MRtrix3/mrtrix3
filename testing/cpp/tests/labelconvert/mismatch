#!/bin/bash
# Ensure that if there are no matches between the input and output lookup tables,
#   the command throws an exception
# Ensure that output matches that generated by a prior software version
! labelconvert labelconvert/in_aal.mif.gz labelconvert/ROI_MNI_V4.txt labelconvert/fs_default.txt tmp.mif -force
