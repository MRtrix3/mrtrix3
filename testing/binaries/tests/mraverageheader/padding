#!/bin/bash
# Verify operation when additional padding is added to the average header
# Ensure that output matches that generated by a prior software version
mraverageheader moving.mif.gz template.mif.gz - \
-padding 1 | \
testing_diff_header - mraverageheader/padding.mif.gz
