#!/bin/bash
# Verify operation when the voxel spacing is determined based on the "min_nearest" calculation
# Ensure that output matches that generated by a prior software version
mraverageheader moving.mif.gz template.mif.gz - \
-spacing min_nearest | \
testing_diff_header - mraverageheader/min_nearest.mif.gz
