#!/bin/bash
# Verify operation when the output voxel spacing is determined using the "min_projection" calculation
# Ensure that output matches that generated by a prior software version
mraverageheader moving.mif.gz template.mif.gz - \
-spacing min_projection | \
testing_diff_header - mraverageheader/min_projection.mif.gz
