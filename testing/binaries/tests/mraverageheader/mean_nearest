#!/bin/bash
# Verify operation when the output header voxel spacing is based on the "mean_nearest" calculation
# Ensure that output matches that generated by a prior software version
mraverageheader moving.mif.gz template.mif.gz - \
-spacing mean_nearest | \
testing_diff_header - mraverageheader/mean_nearest.mif.gz
