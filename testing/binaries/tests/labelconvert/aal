#!/bin/bash
# Verify operation when taking as input the AAL parcellation in its native form,
#   and serialising the indices using a destination lookup table
# Ensure that output matches that generated by a prior software version
labelconvert labelconvert/in_aal.mif.gz labelconvert/ROI_MNI_V4.txt labelconvert/aal.txt - | \
testing_diff_image - labelconvert/out_aal.mif.gz
