#!/bin/bash
# Verify operation when input is a FreeSurfer parcellation image,
#   and the desired parcel indices are serialised using an appropriate destination lookup table
# Ensure that output matches that generated by a prior software version
labelconvert labelconvert/in_fs.mif.gz labelconvert/FreeSurferColorLUT.txt labelconvert/fs_default.txt - | \
testing_diff_image - labelconvert/out_fs.mif.gz
