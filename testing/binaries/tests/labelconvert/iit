#!/bin/bash
# Verify operation when input image is the IIT atlas in its native form,
#   and the indices are serialised based on a lookup file 
#   containing the cortical labels from the FreeSurfer Destrieux Parcellation
# Ensure that output matches that generated by a prior software version
labelconvert labelconvert/in_iit.mif.gz labelconvert/LUT_GM.txt labelconvert/fs_a2009s.txt - | \
testing_diff_image - labelconvert/out_iit.mif.gz
