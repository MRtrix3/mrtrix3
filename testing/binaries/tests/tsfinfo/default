#!/bin/bash
# Verify default operation of "tsfinfo" on a well-formed track scalar file.
# Checks that key fields are reported in the output.

rm -f tmp.txt
tsfinfo afd.tsf 2>&1 | tee tmp.txt
grep "Track scalar file:" tmp.txt
grep -E "count:" tmp.txt
