#!/bin/bash
# Verify operation when taking a FreeSurfer parcellation image
#   and also providing the FreeSurfer colour lookup table,
#   such that the resulting RGB image has colours consistent with FreeSurfer
# Ensure that output matches that generated by a prior software version
label2colour labelconvert/in_fs.mif.gz - \
-lut labelconvert/FreeSurferColorLUT.txt | \
testing_diff_image - label2colour/out_fs.mif.gz
