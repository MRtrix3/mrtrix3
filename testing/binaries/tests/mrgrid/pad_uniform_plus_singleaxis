#!/bin/bash
# Evaluate the "pad" operation when two options are used in conjunction:
# - "-axis", with custom padding along one specific axis
# - "-uniform", applying uniform padding to axes not otherwise explicitly specified
# Outcome is verified against that generated by a prior software version
mrgrid dwi.mif pad -axis 0 1,2 -uniform 3 - | \
testing_diff_image - mrpad/out3.mif
