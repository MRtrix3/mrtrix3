#!/bin/bash
# Verify "regrid" operation when performing upsampling (via -scale) option,
#   but using linear interpolation rather than the default
# Check the output against that generated by an obsolete command (mrresize)
#   from an earlier version of the software
mrgrid dwi.mif regrid -scale 1.6 -datatype float32 -interp linear - | \
testing_diff_image - mrresize/out3.mif -voxel 1e-5
