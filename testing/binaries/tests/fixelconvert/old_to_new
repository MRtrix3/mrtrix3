#!/bin/bash
# Verify operation when converting an old "MRtrix Sparse Format (.msf)" image to the new fixel directory format
# Ensure that output matches that generated by a prior software version
fixelconvert old_fixel.msf tmp
testing_diff_fixel tmp fixelconvert
