#!/bin/bash
# Verify operation when converting a fixel data file as stored in the new fixel directory format
#   to the legacy ".msf" format
# Ensure that output matches that generated by a prior software version
fixelconvert fixel_image tmp.msf -value fixel_image/afd.mif
testing_diff_fixel_old tmp.msf old_fixel.msf 0.001
