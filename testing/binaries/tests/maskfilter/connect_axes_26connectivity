#!/bin/bash
# Verify operation of the connected components filter
#   when two different modifications are applied:
#   - Restriction of the axes along which voxel adjacency can expand selection of a cluster
#   - Use 26-nearest-neighbourhood connectivity rather than 6
# Ensure that output matches that generated by a prior software version
maskfilter components_mask.mif connect -axes 0,2 -connectivity - | \
testing_diff_image - maskfilter/out6.mif
