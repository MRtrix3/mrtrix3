#!/bin/bash
# Verify operation of the mask erosion filter
#   when voxel adjacency is determined using 26 nearest neighbours rather than only 6
# Ensure that output matches that generated by a prior software version
maskfilter mask.mif erode -npass 2 -connectivity - | \
testing_diff_image - maskfilter/out11.mif
