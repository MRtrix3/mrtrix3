#!/bin/bash
# Verify operation of the connected components filter
#   when only the largest connected component is extracted
# Ensure that output matches that generated by a prior software version
maskfilter components_mask.mif connect -largest - | \
testing_diff_image - maskfilter/out2.mif
