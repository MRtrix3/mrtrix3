#!/bin/bash
# Verify operation of command when using a range of different command-line options,
#   including panchromatic sharpening using the -contrast option.
# Ensure that output matches that generated by a prior software version
fod2dec fod2dec/fod.mif - \
-contrast fod2dec/anat.mif \
-mask fod2dec/roi.mif \
-threshold 0.07 \
-lum | \
testing_diff_image - fod2dec/out/dec-anat_combo.mif -voxel 1e-5
