#!/bin/bash
# Verify operation when performing panchromatic sharpening using the -contrast option,
#   and also normalising the directionally-encoded colours
# Ensure that output matches that generated by a prior software version
fod2dec fod2dec/fod.mif - \
-contrast fod2dec/anat.mif \
-no_weight | \
testing_diff_image - fod2dec/out/dec-anat_noweig.mif -voxel 1e-5
