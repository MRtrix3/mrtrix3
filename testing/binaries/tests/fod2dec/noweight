#!/bin/bash
# Verify operation when the "-noweight" option is specified
# Ensure that output matches that generated by a prior software version
# TODO Test that output image has unit norm
fod2dec fod2dec/fod.mif - -no_weight | \
testing_diff_image - fod2dec/out/dec_noweig.mif -voxel 1e-5
