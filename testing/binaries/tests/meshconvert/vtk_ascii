#!/bin/bash
# Verify write and then read of a VTK in ASCII mode
# Note that this test does not necessarily ensure validity of the ASCII VTK;
#   only that the write and read operations work and are self-compatible
# Ensure that output matches that generated by a prior software version
meshconvert meshconvert/in_ascii.vtk tmp.vtk -force
testing_diff_mesh tmp.vtk meshconvert/in_ascii.vtk 0.001
