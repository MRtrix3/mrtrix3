#!/bin/bash
# Verify write and read operations for STL format in binary mode
# Note that this test does not necessarily guarantee compliance of the STL object;
#   only that the write and read operations aork and are self-consistent
# Ensure that output matches that generated by a prior software version
meshconvert meshconvert/in_ascii.vtk tmp.stl -binary -force
testing_diff_mesh tmp.stl meshconvert/in_ascii.vtk 0.001
