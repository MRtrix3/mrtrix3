#!/bin/bash
# Verify support for the OBJ format in binary mode
# Note that this test does not necessarily guarantee compiance of the binary OBJ object;
#   only that the write and read operations function are are self-consistent
# Ensure that output matches that generated by a prior software version
meshconvert meshconvert/in_ascii.vtk tmp.obj -binary -force
testing_diff_mesh tmp.obj meshconvert/in_ascii.vtk 0.001
